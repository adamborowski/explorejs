import React from 'react';
// Since this component is simple and static, there's no parent container for it.
const ScenarioPage = () => {
  return (
    <div>
      <h1 className="page-header">Scenarios</h1>
    </div>
  );
};

export default ScenarioPage;
