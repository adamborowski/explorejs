/**
 * more smart component which refers to global managers and allows to reuse cache/managers across all charting adapters in application
 * it checks uniqueness by comparing manifest and batch api
 */
